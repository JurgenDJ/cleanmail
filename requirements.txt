# Note: pyproject.toml is the main package manager for this project.
# This requirements.txt file is kept for compatibility.
# To install dependencies, use: uv sync (or pip install -r requirements.txt)
streamlit==1.39.0
pandas==2.2.3
beautifulsoup4==4.12.3
python-dotenv==1.0.0
